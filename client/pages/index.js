import React from 'react';
import MessageList from '../components/messageList';

const Home = () => (
  <div>
    <h2>NextJS + Apollo + GraphQL + MongoDB Starter</h2>
    <MessageList></MessageList>
  </div>
);

export default Home;
